{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "C/C++: clang++ debug",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/src/driver",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "C/C++: clang++ build active file"
        }
    ]
}